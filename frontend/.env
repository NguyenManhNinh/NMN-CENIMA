# CẤU HÌNH SERVER (BACKEND)
# Đường dẫn gốc API (Backend chạy port 5000)
# Lưu ý: Không thêm dấu / ở cuối để tránh lỗi nối chuỗi
VITE_API_BASE_URL=http://localhost:5000/api

# Đường dẫn Socket.io (Dùng cho tính năng giữ ghế Realtime)
# Lưu ý: Socket kết nối vào gốc server, không phải vào /api
VITE_SOCKET_URL=http://localhost:5000

# CẤU HÌNH ỨNG DỤNG

# Tên hiển thị trên web/tab trình duyệt
VITE_APP_TITLE=Cinema Booking System

# Môi trường (dev/prod)
VITE_NODE_ENV=development

# CẤU HÌNH THANH TOÁN & SOCIAL

# Google Client ID (Dành cho chức năng Login Google)
# Để trống nếu chưa đăng ký, điền sau cũng được
VITE_GOOGLE_CLIENT_ID=

# Đường dẫn trang kết quả thanh toán (Frontend Router)
# Khi VNPay trả về, Frontend sẽ hứng ở trang này
VITE_PAYMENT_RETURN_URL=/payment/result